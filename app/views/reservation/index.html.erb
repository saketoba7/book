<div class="room-index reservations">
    
            <% @reservation. each do |r| %>
                <tr>
                    <th><%= image_tag r.room.images.url class: "index-img"  %></th>
                    <th><%= link_to r.room.room_name,reservation_path(r) %></th>
                    <th><%= r.room.room_PR %></th>
                    <th><%= r.room.room_price * ((r.end_date.to_date - r.start_date.to_date).to_i) %></th>
                    <th><%= r.start_date %></th>
                    <th><%= r.end_date %></th>
                    <th><%= r.created_at %></th>
                </tr>
            <%end%>    
        
</div>